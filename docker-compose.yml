version: '3.8'
services:
  db:
    image: postgres:15-alpine
    security_opt:
      - seccomp:unconfined
    environment:
      POSTGRES_DB: salon
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

